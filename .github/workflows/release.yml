name: Release packages

on:
  workflow_dispatch:
    inputs:
      version:
        description: Release version without v prefix (e.g. 3.2.2)
        required: true
        type: string
      push_images:
        description: Push Docker images to GHCR and Aliyun ACR
        required: false
        type: boolean
        default: true
  push:
    tags:
      - "v*"

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: write
  packages: write

env:
  NIUMA_UI_VERSION: "1.1.3"
  ALIYUN_REGISTRY: crpi-25xt72cd1prwdj5s.cn-hangzhou.personal.cr.aliyuncs.com
  ALIYUN_NAMESPACE: datahub-images

jobs:
  prepare:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.meta.outputs.version }}
      push_images: ${{ steps.meta.outputs.push_images }}
      is_tag: ${{ steps.meta.outputs.is_tag }}
      build_date: ${{ steps.meta.outputs.build_date }}
    steps:
      - id: meta
        shell: bash
        run: |
          set -euo pipefail
          if [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            VERSION="${{ github.event.inputs.version }}"
            PUSH_IMAGES="${{ github.event.inputs.push_images }}"
            IS_TAG="false"
          else
            VERSION="${GITHUB_REF_NAME#v}"
            PUSH_IMAGES="true"
            IS_TAG="true"
          fi
          if [[ -z "$VERSION" ]]; then
            echo "version is empty" >&2
            exit 1
          fi
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"
          echo "push_images=${PUSH_IMAGES}" >> "$GITHUB_OUTPUT"
          echo "is_tag=${IS_TAG}" >> "$GITHUB_OUTPUT"
          echo "build_date=$(date -u +"%Y-%m-%dT%H:%M:%SZ")" >> "$GITHUB_OUTPUT"

  frontend:
    needs: prepare
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: pnpm/action-setup@v4
        with:
          version: 9
      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: web/frontend/pnpm-lock.yaml
      - name: Install frontend deps
        working-directory: web/frontend
        run: |
          pnpm pkg set "dependencies.niuma-ui=${NIUMA_UI_VERSION}"
          # niuma-ui styles.css 的 @import "tailwindcss" 依赖宿主安装 Tailwind v4
          pnpm add -D tailwindcss@^4.3.2 @tailwindcss/vite@^4.3.2
          pnpm install --no-frozen-lockfile
      - name: Build frontend
        working-directory: web/frontend
        run: pnpm build
      - uses: actions/upload-artifact@v7
        with:
          name: frontend-dist
          path: web/frontend/dist
          if-no-files-found: error
          retention-days: 7

  package-linux:
    needs: [prepare, frontend]
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - id: standard
            oracle: "0"
          - id: oracle
            oracle: "1"
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: true
      - uses: actions/download-artifact@v8
        with:
          name: frontend-dist
          path: web/frontend/dist
      - name: Install host tools
        run: sudo apt-get update && sudo apt-get install -y --no-install-recommends unzip
      - uses: actions/cache@v5
        if: matrix.oracle == '1'
        with:
          path: .oracle
          key: oracle-instantclient-21.18-linux-amd64
      - name: Download Oracle Instant Client
        if: matrix.oracle == '1'
        run: bash .github/scripts/download-oracle-linux.sh
      - name: Package
        env:
          VERSION: ${{ needs.prepare.outputs.version }}
          ORACLE: ${{ matrix.oracle }}
        run: bash .github/scripts/package-linux-ci.sh
      - uses: actions/upload-artifact@v7
        with:
          name: package-linux-${{ matrix.id }}
          path: dist/gateway-*.tar.gz
          if-no-files-found: error
          retention-days: 14

  package-windows:
    needs: [prepare, frontend]
    runs-on: windows-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - id: standard
            oracle: "0"
          - id: oracle
            oracle: "1"
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: true
      - uses: actions/download-artifact@v8
        with:
          name: frontend-dist
          path: web/frontend/dist
      - uses: egor-tensin/setup-mingw@v3
        with:
          platform: x64
      - uses: ilammy/msvc-dev-cmd@v1
        if: matrix.oracle == '1'
      - uses: actions/cache@v5
        if: matrix.oracle == '1'
        with:
          path: .oracle
          key: oracle-instantclient-21.18-windows-amd64
      - name: Download Oracle Instant Client
        if: matrix.oracle == '1'
        shell: pwsh
        run: .github/scripts/download-oracle-windows.ps1
      - name: Package
        env:
          VERSION: ${{ needs.prepare.outputs.version }}
          ORACLE: ${{ matrix.oracle }}
          CC: gcc
        shell: pwsh
        run: .github/scripts/package-windows.ps1
      - uses: actions/upload-artifact@v7
        with:
          name: package-windows-${{ matrix.id }}
          path: dist/gateway-*.zip
          if-no-files-found: error
          retention-days: 14

  docker:
    needs: [prepare, frontend]
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - id: standard
            file: scripts/docker/Dockerfile
            suffix: ""
            latest: "true"
          - id: oracle
            file: scripts/docker/Dockerfile.oracle
            suffix: "-oracle"
            latest: "false"
    steps:
      - uses: actions/checkout@v5
      - uses: actions/download-artifact@v8
        with:
          name: frontend-dist
          path: web/frontend/dist
      - uses: docker/setup-buildx-action@v4
      - name: Resolve image names
        id: images
        env:
          ALIYUN_USER: ${{ secrets.ALIYUN_USERNAME }}
          ALIYUN_PASS: ${{ secrets.ALIYUN_PASSWORD }}
        run: |
          set -euo pipefail
          LIST="ghcr.io/${{ github.repository }}"
          ALIYUN=false
          if [ -n "${ALIYUN_USER}" ] && [ -n "${ALIYUN_PASS}" ]; then
            LIST="${LIST}"$'\n'"${ALIYUN_REGISTRY}/${ALIYUN_NAMESPACE}/gateway"
            ALIYUN=true
          else
            echo "::warning::ALIYUN_USERNAME / ALIYUN_PASSWORD 未配置，跳过阿里云 ACR 推送"
          fi
          echo "aliyun=${ALIYUN}" >> "$GITHUB_OUTPUT"
          {
            echo "list<<EOF"
            printf '%s\n' "${LIST}"
            echo "EOF"
          } >> "$GITHUB_OUTPUT"
      - uses: docker/login-action@v4
        if: needs.prepare.outputs.push_images == 'true'
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/login-action@v4
        if: needs.prepare.outputs.push_images == 'true' && steps.images.outputs.aliyun == 'true'
        with:
          registry: ${{ env.ALIYUN_REGISTRY }}
          username: ${{ secrets.ALIYUN_USERNAME }}
          password: ${{ secrets.ALIYUN_PASSWORD }}
      - uses: docker/metadata-action@v6
        id: meta
        with:
          images: ${{ steps.images.outputs.list }}
          tags: |
            type=raw,value=${{ needs.prepare.outputs.version }}${{ matrix.suffix }}
            type=raw,value=latest,enable=${{ matrix.latest == 'true' && needs.prepare.outputs.is_tag == 'true' }}
      - uses: docker/build-push-action@v7
        with:
          context: .
          file: ${{ matrix.file }}
          push: ${{ needs.prepare.outputs.push_images == 'true' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: |
            VERSION=${{ needs.prepare.outputs.version }}
            BUILD_DATE=${{ needs.prepare.outputs.build_date }}
            GIT_COMMIT=${{ github.sha }}
            ALPINE_MIRROR=
            DEBIAN_MIRROR=
          cache-from: type=gha,scope=docker-${{ matrix.id }}
          cache-to: type=gha,mode=min,scope=docker-${{ matrix.id }}
          # 阿里云个人版 ACR 不识别 OCI 1.1 空描述符（provenance/SBOM 会带上）
          provenance: false
          sbom: false

  release:
    needs: [prepare, package-linux, package-windows]
    if: needs.prepare.outputs.is_tag == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v8
        with:
          pattern: package-*
          merge-multiple: true
          path: artifacts
      - uses: softprops/action-gh-release@v2
        with:
          files: artifacts/*
          generate_release_notes: true
