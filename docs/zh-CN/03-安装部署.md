# FLUX Gateway - 安装部署

本文档提供 FLUX Gateway 的安装部署指南，包括环境要求、安装方式、目录结构和部署配置。

---

## 📋 环境要求

### 系统要求

#### 最低配置
- **操作系统**: Linux、macOS 或 Windows
- **CPU**: 1核
- **内存**: 256MB
- **磁盘空间**: 10GB
- **网络**: 根据配置需要开放相应端口

#### 推荐配置
- **CPU**: 2核及以上
- **内存**: 512MB 及以上
- **磁盘空间**: 20GB 及以上
- **网络**: 稳定的网络连接

### 软件依赖

#### 必需软件
- **数据库**（三选一）:
  - MySQL 5.7+ / 8.0+
  - Oracle 11g+ / 12c+ （需要安装 Oracle Instant Client）
  - SQLite 3.x

#### 可选软件
- **Redis**: 6.0+ （用于缓存和分布式限流）
- **MongoDB**: 4.4+ （用于文档存储）
- **ClickHouse**: 21.0+ （用于日志分析）

### Oracle Instant Client 安装（使用 Oracle 数据库时必需）

如果选择使用 Oracle 数据库，需要安装 Oracle Instant Client。

#### 下载 Oracle Instant Client

访问 Oracle 官方下载页面：

- **下载地址**: https://www.oracle.com/database/technologies/instant-client/linux-x86-64-downloads.html
- 下载 **Instant Client for Linux x86-64** 的基础包（Basic Package）
- 建议下载最新稳定版本（如 21.x 或 19.x）

#### 安装步骤（Linux）

```bash
# 1. 创建安装目录
sudo mkdir -p /opt/oracle
cd /opt/oracle

# 2. 下载并解压 Instant Client（以 21.18 版本为例）
# 注意：需要先注册 Oracle 账号才能下载
wget https://download.oracle.com/otn_software/linux/instantclient/2118000/instantclient-basic-linux.x64-21.18.0.0.0dbru.zip
unzip instantclient-basic-linux.x64-21.18.0.0.0dbru.zip

# 3. 配置环境变量（临时生效，仅当前会话）
export ORACLE_HOME=/opt/oracle/instantclient_21_18
export LD_LIBRARY_PATH=$ORACLE_HOME/lib:$LD_LIBRARY_PATH

# 4. 配置环境变量（永久生效）
# 编辑 ~/.bashrc 或 ~/.bash_profile
echo 'export ORACLE_HOME=/opt/oracle/instantclient_21_18' >> ~/.bashrc
echo 'export LD_LIBRARY_PATH=$ORACLE_HOME/lib:$LD_LIBRARY_PATH' >> ~/.bashrc
source ~/.bashrc

# 5. 验证安装
ls -l $ORACLE_HOME/lib/libclntsh.so*
```

> 💡 **说明**：
> - **为什么不能打包 Oracle Instant Client？**
>   - Oracle Instant Client 是 Oracle 公司的专有软件，受许可协议限制
>   - Oracle 的许可协议**不允许将 Oracle Instant Client 重新分发或打包**到其他软件中
>   - 用户必须从 Oracle 官网单独下载并同意许可协议
>   - 这是法律和许可要求，不是技术限制
> - **Windows 和 Linux 的区别：**
>   - Windows 服务会继承系统环境变量（如 `PATH`），如果 Oracle Instant Client 安装在系统 `PATH` 中，服务可以自动找到 `oci.dll`
>   - Linux systemd 服务**不会自动继承用户环境变量**（如 `LD_LIBRARY_PATH`），这是 systemd 的安全特性
>   - 因此 Linux 需要在 systemd 服务文件中显式设置 `LD_LIBRARY_PATH` 环境变量
> - **其他说明：**
>   - Oracle Instant Client 需要 Oracle 账号才能下载（免费注册）
>   - 如果使用 systemd 服务，安装脚本会自动检测并配置 `LD_LIBRARY_PATH`
>   - 环境变量配置后需要重新登录或执行 `source ~/.bashrc` 才能生效
>   - 如果使用 Oracle 版本的二进制文件，必须配置环境变量，否则程序无法启动

---

## 📦 获取安装包

### 方式一：下载预编译版本（推荐）

从 GitHub Releases 下载已编译好的版本：

```bash
# 访问 Releases 页面
https://github.com/fluxsce/gateway/releases

# 下载对应平台的版本
# Linux 64位
wget https://github.com/fluxsce/gateway/releases/download/v2.0.1/gateway-linux-amd64.tar.gz

# Windows 64位
# 下载 gateway-windows-amd64.zip

# macOS 64位
wget https://github.com/fluxsce/gateway/releases/download/v2.0.1/gateway-darwin-amd64.tar.gz
```

**解压安装包**

```bash
# Linux/macOS
tar -xzf gateway-linux-amd64.tar.gz
cd gateway

# Windows
# 使用解压工具解压 gateway-windows-amd64.zip
```

### 方式二：自行编译

如果需要自定义编译或支持 Oracle 数据库，请参考 [开发指南](./02-快速开始.md#编译构建) 进行编译。

#### 编译产物说明

使用官方构建脚本编译后，会在 `dist/` 目录生成完整的部署包。所有构建脚本统一输出到 `dist/gateway/` 目录，包含可执行文件、配置文件、Web 资源和部署脚本。

**Windows 平台构建脚本**

```cmd
# 进入构建脚本目录
cd scripts\build

# Windows 10/11 版本（默认仅 MySQL/SQLite，不支持 Oracle）
build-win10.cmd
# 或指定参数
build-win10.cmd --oracle    # 启用 Oracle 支持
build-win10.cmd --no-oracle # 禁用 Oracle 支持（默认，仅 MySQL/SQLite）

# 生成目录结构：
# dist/
# └── gateway/
#     ├── gateway.exe          # 可执行文件
#     ├── configs/             # 配置文件目录
#     ├── web/                 # Web 资源目录
#     │   ├── static/          # 后端静态资源
#     │   └── frontend/dist/   # 前端打包资源
#     ├── scripts/             # 脚本目录
#     │   ├── db/             # 数据库脚本
#     │   └── deploy/         # 部署脚本
#     ├── logs/               # 日志目录（自动创建）
#     └── backup/             # 备份目录
```

**Linux 平台构建脚本**

```bash
# 进入构建脚本目录
cd scripts/build

# CentOS 7 兼容版本（默认仅 MySQL/SQLite，不支持 Oracle）
./build-centos7.sh

# 启用 Oracle 支持
./build-centos7.sh --oracle

# 生成目录结构：
# dist/
# └── gateway/
#     ├── gateway              # 可执行文件
#     ├── configs/             # 配置文件目录
#     ├── web/                 # Web 资源目录
#     │   ├── static/          # 后端静态资源
#     │   └── frontend/dist/   # 前端打包资源
#     ├── scripts/             # 脚本目录
#     │   ├── db/             # 数据库脚本
#     │   └── deploy/         # 部署脚本
#     ├── logs/               # 日志目录（自动创建）
#     └── backup/             # 备份目录
```

> 💡 **说明**：
> - 所有构建脚本统一输出到 `dist/gateway/` 目录，包含完整的部署结构
> - **默认构建仅支持 MySQL/SQLite，不包含 Oracle 支持**
> - 如需启用 Oracle 支持，Windows 使用 `--oracle` 参数，Linux 使用 `./build-centos7.sh --oracle`
> - 启用 Oracle 支持需要预先安装 Oracle Instant Client 并设置 `ORACLE_HOME` 环境变量
> - 如果 Oracle 环境不完整，构建脚本会停止执行并提示错误信息
> - 构建脚本会自动检查 Oracle 环境，确保构建的二进制文件可以正常使用

```bash
# 克隆项目
git clone https://github.com/fluxsce/gateway.git
cd gateway

# 查看所有可用的构建脚本
ls scripts/build/
```

---

## 📁 标准目录结构

正常的网关部署目录结构如下：

```
gateway/                          # 部署根目录
├── gateway                       # 可执行文件（Linux/macOS）
├── gateway.exe                   # 可执行文件（Windows）
├── configs/                      # 配置文件目录
│   ├── app.yaml                  # 应用配置
│   ├── database.yaml             # 数据库配置
│   ├── gateway.yaml              # 网关配置
│   ├── logger.yaml               # 日志配置
│   └── web.yaml                  # Web控制台配置
├── web/                          # Web资源目录
│   ├── static/                   # 后端静态资源
│   └── frontend/                 # Vue3前端项目
│       └── dist/                 # 前端打包后的静态资源
│           ├── index.html        # 主页面
│           ├── favicon.ico       # 网站图标
│           └── assets/           # 静态资源（CSS、JS、图片等）
├── logs/                         # 日志目录（自动创建）
│   ├── debug.log                 # 调试日志
│   ├── info.log                  # 信息日志
│   ├── warn.log                  # 警告日志
│   └── error.log                 # 错误日志
├── backup/                       # 备份目录（可选）
├── pprof_analysis/               # 性能分析目录（可选）
├── scripts/                      # 脚本目录（开发环境）
│   ├── build/                    # 构建脚本
│   │   ├── build.cmd             # Windows 构建菜单
│   │   ├── build-win10-oracle.cmd
│   │   ├── build-win2008-oracle.cmd
│   │   ├── build-win10-centos7.cmd
│   │   ├── build-centos7.sh
│   │   └── ...                   # 其他构建脚本
│   ├── deploy/                   # 部署脚本
│   │   ├── install-service.cmd   # Windows 服务安装
│   │   ├── uninstall-service.cmd # Windows 服务卸载
│   │   └── ...                   # 其他部署脚本
│   ├── db/                       # 数据库脚本
│   │   ├── frp_tunnel_management.sql         # MySQL
│   │   ├── frp_tunnel_management_oracle.sql  # Oracle
│   │   └── frp_tunnel_management_sqlite.sql  # SQLite
│   └── test/                     # 测试脚本
└── README.md                     # 说明文档
```

> 💡 **说明**：
> - `scripts/` 目录通常只在开发环境中存在，生产部署时可以不包含
> - 如果从源码编译，建议保留 `scripts/deploy/` 用于服务管理
> - 如果下载预编译版本，通常只包含可执行文件、配置文件和 Web 资源

---

## ⚙️ 配置说明

数据库初始化和配置文件说明请参考 [开发指南](./02-快速开始.md) 章节：
- [数据库初始化](./02-快速开始.md#数据库初始化)
- [配置文件](./02-快速开始.md#配置文件)

---

## 🚀 部署和启动

| 部署方式 | 适用场景 | 优点 | 推荐度 |
|---------|---------|------|--------|
| **使用安装包部署** | 生产环境、快速部署 | 开箱即用、无需编译环境 | ⭐⭐⭐⭐⭐ |
| 自行编译部署 | 定制化需求、特殊环境 | 灵活配置、支持 Oracle | ⭐⭐⭐ |

| 启动方式 | 适用场景 | 开机自启 | 自动重启 | 推荐度 |
|---------|---------|---------|---------|--------|
| **注册为系统服务** | 生产环境 | ✅ 支持 | ✅ 支持 | ⭐⭐⭐⭐⭐ |
| 直接执行文件 | 开发测试 | ❌ 不支持 | ❌ 不支持 | ⭐⭐ |

---

## 📦 方式一：使用安装包部署（推荐）

### 1. 解压安装包

```bash
# Linux/macOS
tar -xzf gateway-linux-amd64.tar.gz -C /opt/
cd /opt/gateway

# Windows
# 解压到 C:\gateway
cd C:\gateway
```

解压后的完整目录结构已包含所有必需文件（可执行文件、配置文件、Web 资源、部署脚本）。

### 2. 配置数据库和应用

根据实际环境修改配置文件，详见 [开发指南 - 配置文件](./02-快速开始.md#配置文件)。

### 3. 启动应用

#### 方式 A：注册为系统服务启动（推荐）

**Windows 服务**

```cmd
# 使用官方部署脚本
scripts\deploy\install-service.cmd

# Oracle 版本
scripts\deploy\install-service.cmd oracle

# 启动服务
sc start Gateway

# 查看服务状态
sc query Gateway
```

**Linux Systemd 服务**

```bash
# 使用官方部署脚本
sudo scripts/deploy/install-service-linux.sh

# 启动服务
sudo systemctl start gateway

# 查看状态
sudo systemctl status gateway.service
```

#### 方式 B：直接执行文件启动

**Linux/macOS**

```bash
# 前台启动
./gateway

# 后台启动
nohup ./gateway > logs/app.log 2>&1 &
```

**Windows**

```cmd
REM 前台启动
gateway.exe

REM 后台启动
start /B gateway.exe
```

---

## 🔨 方式二：自行编译部署

适用于需要定制化编译或支持 Oracle 数据库的场景。

### 1. 编译应用

参考 [开发指南 - 编译构建](./02-快速开始.md#编译构建) 进行编译。

### 2. 创建部署目录

```bash
sudo mkdir -p /opt/gateway/{configs,logs,web/frontend/dist,web/static}
```

### 3. 复制文件

构建脚本已自动生成完整的部署包，直接复制整个目录即可：

```bash
# 复制整个部署包
sudo cp -r dist/gateway /opt/

# 设置可执行权限
sudo chmod +x /opt/gateway

# 或者重命名目录
sudo mv /opt/gateway /opt/gateway-app
```

> 💡 **说明**：
> - 构建脚本已自动打包所有必需文件（可执行文件、配置文件、Web 资源、部署脚本）
> - 无需手动复制各个文件，直接使用 `dist/gateway/` 目录即可
> - 如果从源码编译，建议保留 `scripts/deploy/` 用于服务管理

### 4. 配置数据库和应用

根据实际环境修改配置文件，详见 [开发指南 - 配置文件](./02-快速开始.md#配置文件)。

### 5. 启动应用

#### 方式 A：注册为系统服务启动（推荐）

```bash
# 使用官方部署脚本
sudo scripts/deploy/install-service-linux.sh
```

#### 方式 B：直接执行文件启动

```bash
cd /opt/gateway
./gateway
```

---

## ✅ 验证部署

### 1. 检查进程

```bash
# Linux/macOS
ps aux | grep gateway

# Windows
tasklist | findstr gateway
```

### 2. 检查端口

```bash
# Linux/macOS
netstat -tulpn | grep :8080
netstat -tulpn | grep :12003

# Windows
netstat -ano | findstr :8080
netstat -ano | findstr :12003
```

### 3. 访问 Web 控制台

打开浏览器访问：

- **控制台地址**: http://localhost:12003/gatewayweb
- **默认用户名**: `admin`
- **默认密码**: `123456`

---

## 📖 下一步

部署完成后，建议继续阅读：

- [项目介绍](./01-项目介绍.md) - 了解项目架构和核心能力
- [开发指南](./02-快速开始.md) - 开发环境搭建和配置说明
- [容器化部署](./04-容器化部署.md) - Docker 和 Kubernetes 部署

---

## 🤝 获取帮助

如果遇到部署问题，可以通过以下方式获取帮助：

- 📧 邮箱: fluxopensource@flux.com.cn
- 💬 GitHub Issues: [提交问题](https://github.com/fluxsce/gateway/issues)
- 📚 查看完整文档: [GitHub 仓库](https://github.com/fluxsce/gateway)

---

<div align="center">

**[返回目录](./README.md) • [上一章：开发指南](./02-快速开始.md) • [下一章：容器化部署](./04-容器化部署.md)**

Made with ❤️ by FLUX Gateway Team

</div>

