# FLUX Gateway - 开发指南

本文档提供 FLUX Gateway 的开发环境搭建、编译构建和快速启动指南，帮助开发者快速上手项目开发。

---

## 🔧 开发环境搭建

### 1. 安装 Go 环境

```bash
# 检查 Go 版本
go version  # 需要 Go 1.24+

# 如果未安装，请访问 https://golang.org/dl/ 下载安装
```

**国内用户配置 Go 代理（推荐）**

```bash
go env -w GOPROXY=https://goproxy.cn,direct
go env -w GOSUMDB=sum.golang.google.cn
```

### 2. Windows 环境特殊配置

由于项目依赖 CGO（特别是 Oracle 支持），Windows 用户需要安装 C 编译器：

#### 安装 TDM-GCC（推荐）

1. 访问 [TDM-GCC 下载页面](https://jmeubank.github.io/tdm-gcc/download/)
2. 下载：`tdm64-gcc-10.3.0-2.exe`（64+32位 MinGW-w64 版本）
3. 安装完成后**重新打开命令行窗口**
4. 验证安装：
   ```cmd
   gcc --version
   ```

#### 其他可选编译器

- **Microsoft Visual Studio Build Tools**
- **MinGW-w64**

### 3. Oracle 支持环境配置（可选）

如果需要 Oracle 数据库支持，需要额外配置：

#### Windows 环境

1. **下载 Oracle Instant Client**
   - Basic Package: [instantclient-basic-windows.x64-21.18.0.0.0dbru.zip](https://www.oracle.com/database/technologies/instant-client/winx64-64-downloads.html)
   - SDK Package: [instantclient-sdk-windows.x64-21.18.0.0.0dbru.zip](https://www.oracle.com/database/technologies/instant-client/winx64-64-downloads.html)

2. **安装步骤**
   ```cmd
   # 创建目标目录
   mkdir D:\SDK\instantclient_21_18
   
   # 解压两个 zip 包到同一目录
   # 确保目录结构：
   # D:\SDK\instantclient_21_18\
   # ├── oci.dll
   # ├── sdk\
   # │   ├── include\
   # │   │   └── oci.h
   # │   └── lib\
   # │       └── msvc\
   # │           └── oci.lib
   ```

3. **配置环境变量**
   ```cmd
   # 设置环境变量
   setx ORACLE_HOME "D:\SDK\instantclient_21_18"
   setx PATH "%ORACLE_HOME%;%PATH%"
   setx CGO_ENABLED "1"
   
   # 或使用官方脚本
   cd scripts\build
   setup-oracle-env.cmd
   ```

#### Linux 环境

```bash
# 1. 下载并安装 Oracle Instant Client
sudo mkdir -p /opt/oracle
cd /opt/oracle
sudo unzip instantclient-basic-linux.x64-21.18.0.0.0dbru.zip
sudo unzip instantclient-sdk-linux.x64-21.18.0.0.0dbru.zip

# 2. 创建符号链接
cd /opt/oracle/instantclient_21_18
sudo ln -s libclntsh.so.21.1 libclntsh.so
sudo ln -s libocci.so.21.1 libocci.so

# 3. 配置环境变量（添加到 ~/.bashrc 或 /etc/profile）
export ORACLE_HOME=/opt/oracle/instantclient_21_18
export LD_LIBRARY_PATH=$ORACLE_HOME:$LD_LIBRARY_PATH
export CGO_ENABLED=1
```

#### macOS 环境

```bash
# 1. 下载并安装 Oracle Instant Client
sudo mkdir -p /opt/oracle
cd /opt/oracle
unzip instantclient-basic-macos.x64-21.18.0.0.0dbru.zip
unzip instantclient-sdk-macos.x64-21.18.0.0.0dbru.zip

# 2. 配置环境变量（添加到 ~/.zshrc 或 ~/.bash_profile）
export ORACLE_HOME=/opt/oracle/instantclient_21_18
export DYLD_LIBRARY_PATH=$ORACLE_HOME:$DYLD_LIBRARY_PATH
export CGO_ENABLED=1

# 3. 安装 Xcode Command Line Tools
xcode-select --install
```

---

## 📦 获取源码

### 克隆项目

```bash
git clone https://github.com/fluxsce/gateway.git
cd gateway
```

### 项目结构

```
gateway/
├── cmd/                          # 应用程序入口
│   ├── app/                      # 主应用程序
│   ├── cache/                    # 缓存服务
│   ├── gateway/                  # 网关服务
│   └── web/                      # Web管理界面
├── configs/                      # 配置文件
│   ├── app.yaml                  # 应用配置
│   ├── database.yaml             # 数据库配置
│   ├── gateway.yaml              # 网关配置
│   ├── logger.yaml               # 日志配置
│   └── web.yaml                  # Web服务配置
├── internal/                     # 内部包（不对外暴露）
│   ├── gateway/                  # 网关核心实现
│   │   ├── bootstrap/            # 网关启动器
│   │   ├── config/               # 网关配置
│   │   ├── core/                 # 核心引擎
│   │   ├── handler/              # 处理器集合
│   │   └── loader/               # 配置加载器
│   ├── tunnel/                   # 隧道管理
│   ├── registry/                 # 服务注册
│   └── types/                    # 数据类型
├── pkg/                          # 公共包（可对外暴露）
│   ├── cache/                    # 缓存工具包
│   ├── config/                   # 配置工具包
│   ├── database/                 # 数据库工具包
│   ├── logger/                   # 日志工具包
│   └── utils/                    # 通用工具
├── web/                          # Web管理平台
│   ├── frontend/                 # Vue3前端项目
│   ├── middleware/               # Web中间件
│   ├── routes/                   # 路由配置
│   └── views/                    # 业务模块视图
├── scripts/                      # 脚本目录
│   ├── build/                    # 构建脚本
│   ├── deploy/                   # 部署脚本
│   └── db/                       # 数据库脚本
├── test/                         # 测试文件
├── go.mod                        # Go模块定义
└── README.md                     # 项目说明文档
```

---

## 🔨 编译构建

### 1. 安装依赖

```bash
# 下载依赖
go mod download

# 验证依赖
go mod verify

# 整理依赖
go mod tidy
```

### 2. 使用官方构建脚本（推荐）

> 💡 **推荐使用 `scripts/build` 目录下的官方构建脚本**  
> 官方脚本已经过充分测试，支持多平台、多数据库版本的自动化构建，无需手动配置复杂的编译参数。

#### Windows 平台

```cmd
# 进入构建脚本目录
cd scripts\build

# Windows 10 + Oracle 支持
build-win10-oracle.cmd

# Windows 10 + CentOS7 兼容版本
build-win10-centos7.cmd

# Windows 2008 + Oracle 支持
build-win2008-oracle.cmd

# 通用一键打包（推荐）
build.cmd
```

#### Linux 平台

```bash
# 进入构建脚本目录
cd scripts/build

# 跨平台构建（需要 PowerShell 7+）
pwsh ./crossbuild.ps1

# 构建所有平台版本
pwsh ./build-all.ps1

# CentOS7 兼容版本
pwsh ./crossbuild-win2008.ps1
```

#### macOS 平台

```bash
# 进入构建脚本目录
cd scripts/build

# 跨平台构建（需要 PowerShell 7+）
pwsh ./crossbuild.ps1

# 构建所有平台版本
pwsh ./build-all.ps1
```

#### Oracle 环境准备脚本

```cmd
# Windows 环境：自动配置 Oracle 环境变量
cd scripts\build
setup-oracle-env.cmd
```

#### 查看构建产物

```bash
# 构建产物位于 dist/ 目录
ls dist/

# Windows 环境
dir dist\
```

**构建脚本说明**
- 自动检测和配置编译环境
- 自动处理 CGO 和 Oracle 依赖
- 自动优化二进制文件大小
- 支持多平台交叉编译
- 详细的构建日志输出

### 3. 开发调试编译（可选）

如果只是本地开发调试，可以使用简单的 `go build` 命令：

```bash
# 快速编译（不优化）
go build -o bin/gateway cmd/app/main.go

# Windows 环境
go build -o bin/gateway.exe cmd/app/main.go
```

> ⚠️ **注意**：开发调试编译不包含生产优化，不建议用于生产环境部署。

---

## 🗄️ 数据库初始化

### MySQL 数据库

```bash
# 1. 创建数据库
mysql -u root -p -e "CREATE DATABASE gateway CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;"

# 2. 导入表结构
mysql -u root -p gateway < scripts/db/mysql.sql

# 或使用完整脚本
mysql -u root -p gateway < scripts/db/frp_tunnel_management.sql
```

### Oracle 数据库

```bash
# 导入表结构
sqlplus username/password@host:port/service_name @scripts/db/oracle.sql

# 或使用完整脚本
sqlplus username/password@host:port/service_name @scripts/db/frp_tunnel_management_oracle.sql
```

### SQLite 数据库

```bash
# SQLite 会在首次启动时自动创建数据库文件
# 如需手动初始化：
sqlite3 gateway.db < scripts/db/sqlite.sql

# 或使用完整脚本
sqlite3 gateway.db < scripts/db/frp_tunnel_management_sqlite.sql
```

---

## ⚙️ 配置文件

### 1. 应用配置 (configs/app.yaml)

```yaml
app:
  name: "Gateway"
  version: "2.0.0"
  debug: false

server:
  host: "0.0.0.0"
  port: 8080

timer:
  enabled: true
  tenant_ids: ["tenant001"]
```

### 2. 数据库配置 (configs/database.yaml)

```yaml
database:
  type: mysql  # mysql, oracle, sqlite
  host: localhost
  port: 3306
  username: root
  password: your_password
  database: gateway
  
  # SQLite 配置
  # type: sqlite
  # database: ./gateway.db
  
  # Oracle 配置
  # type: oracle
  # host: localhost
  # port: 1521
  # username: gateway
  # password: your_password
  # database: XE  # 服务名或SID
```

### 3. 网关配置 (configs/gateway.yaml)

```yaml
gateway:
  # 监听端口
  port: 8080
  
  # 超时配置
  timeout:
    read: 30s
    write: 30s
    idle: 120s
  
  # 限流配置
  rate_limit:
    enabled: true
    rate: 1000  # 每秒请求数
    burst: 2000 # 突发流量
```

### 4. Web 控制台配置 (configs/web.yaml)

```yaml
web:
  name: "Gateway Web服务"
  port: 12003
  run_mode: "debug"  # debug 或 release
  
  # 静态资源配置
  static:
    path: "./web/static"
    prefix: "/static"
  
  # Vue3前端配置
  frontend:
    path: "./web/frontend/dist"
    prefix: "/"
  
  # CORS跨域配置
  cors:
    allowed_origins: "*"
    allowed_methods: "GET,POST,PUT,DELETE,OPTIONS,PATCH"
    allowed_headers: "Origin,Content-Type,Accept,Authorization"
```

### 5. 日志配置 (configs/logger.yaml)

```yaml
logger:
  level: debug  # debug, info, warn, error
  format: json  # json, text
  output: file  # stdout, file
  
  # 文件输出配置
  file:
    path: logs/
    max_size: 100  # MB
    max_backups: 10
    max_age: 30    # 天
```

---

## 🚀 启动应用

### 1. 开发模式启动

```bash
# 直接运行
go run cmd/app/main.go

# 指定配置目录
go run cmd/app/main.go --config ./configs

# 设置日志级别
export GATEWAY_LOG_LEVEL=debug
go run cmd/app/main.go
```

### 2. 使用编译后的二进制启动

```bash
# Linux/macOS
./bin/gateway

# Windows
bin\gateway.exe

# 后台运行（Linux/macOS）
nohup ./bin/gateway > logs/app.log 2>&1 &
```

### 3. 查看启动日志

启动成功后，您将看到类似以下的输出：

```
[INFO] Gateway starting...
[INFO] Database connected successfully
[INFO] Gateway server started on :8080
[INFO] Web console started on :12003
```

### 4. 访问 Web 控制台

打开浏览器访问：

- **控制台地址**: http://localhost:12003/gatewayweb
- **默认用户名**: `admin`
- **默认密码**: `123456`

---

## 📊 网关请求处理流程

<p align="center">
  <img src="../images/gateway_flow.svg" alt="网关请求处理流程" width="100%">
</p>

### 处理流程说明

网关请求处理采用**责任链模式**，每个请求按顺序经过以下处理阶段：

1. **客户端发起请求** → 请求到达网关
2. **前置处理** → 请求解析、参数验证、协议转换
3. **全局安全控制** → IP 黑白名单、User-Agent 过滤、域名访问控制
4. **全局 CORS 处理** → 跨域配置、预检请求
5. **全局认证鉴权** → JWT、OAuth2.0、API Key 验证
6. **全局限流控制** → 令牌桶、漏桶、滑动窗口限流
7. **路由匹配** → 根据路径、方法匹配对应路由
8. **路由级处理器链** → 路由级认证、限流、自定义插件
9. **服务发现** → 从注册中心获取可用服务实例
10. **负载均衡** → 选择目标服务实例
11. **熔断检查** → 检查服务健康状态
12. **请求转发** → 转发请求到后端服务
13. **响应处理** → 处理后端响应，返回给客户端

**基础设施支撑**：日志记录、监控指标、配置管理、缓存支持、数据持久化

---

## ✅ 验证安装

### 1. 健康检查

```bash
# 检查网关状态
curl http://localhost:8080/health

# 检查 Web 控制台
curl http://localhost:12003/health
```

### 2. 查看进程

```bash
# Linux/macOS
ps aux | grep gateway

# Windows
tasklist | findstr gateway
```

### 3. 查看端口

```bash
# Linux/macOS
netstat -tulpn | grep :8080
lsof -i :8080

# Windows
netstat -ano | findstr :8080
```

### 4. 查看日志

```bash
# 实时查看日志
tail -f logs/debug.log
tail -f logs/error.log

# Windows
type logs\debug.log
```

---

## 🐛 常见问题

### 1. 端口被占用

```bash
# 错误: bind: address already in use
# 解决: 修改端口配置或停止占用端口的进程

# Linux/macOS
lsof -i :8080
kill -9 <PID>

# Windows
netstat -ano | findstr :8080
taskkill /PID <PID> /F
```

### 2. 数据库连接失败

```bash
# 错误: 初始化数据库失败
# 解决: 检查数据库配置、网络连接和权限

# 测试 MySQL 连接
mysql -h localhost -u root -p gateway

# 检查配置文件
cat configs/database.yaml
```

### 3. 权限问题

```bash
# 错误: permission denied
# 解决: 确保程序有执行权限，日志目录有写权限

# Linux/macOS
chmod +x bin/gateway
mkdir -p logs
chmod 755 logs

# Windows
# 右键 -> 属性 -> 安全 -> 编辑权限
```

### 4. CGO 编译错误

```bash
# 错误: cgo: C compiler "gcc" not found
# 解决: 安装 C 编译器

# Windows: 安装 TDM-GCC
# Linux: sudo apt-get install build-essential
# macOS: xcode-select --install
```

### 5. Oracle 相关问题

```bash
# 错误: cannot open shared object file: libclntsh.so
# 解决: 检查 Oracle Instant Client 安装和环境变量

# Linux
export LD_LIBRARY_PATH=/opt/oracle/instantclient_21_18:$LD_LIBRARY_PATH

# macOS
export DYLD_LIBRARY_PATH=/opt/oracle/instantclient_21_18:$DYLD_LIBRARY_PATH

# Windows
set PATH=D:\SDK\instantclient_21_18;%PATH%
```

---

## 📖 下一步

开发环境搭建完成后，建议继续阅读：

- [项目介绍](./01-项目介绍.md) - 了解项目架构和核心能力
- [安装部署](./03-安装部署.md) - 生产环境部署指南
- [容器化部署](./04-容器化部署.md) - Docker 和 Kubernetes 部署

---

## 🤝 获取帮助

如果遇到问题，可以通过以下方式获取帮助：

- 📧 邮箱: fluxopensource@flux.com.cn
- 💬 GitHub Issues: [提交问题](https://github.com/fluxsce/gateway/issues)
- 📚 查看完整文档: [docs/guide/](../guide/)

---

<div align="center">

**[返回目录](./README.md) • [下一章：安装部署](./03-安装部署.md)**

Made with ❤️ by FLUX Gateway Team

</div>
