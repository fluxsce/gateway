/**
 * 告警渠道配置列表类型定义
 * 统一管理业务类型，便于后续重构和维护
 */

// 重新导出需要的类型
export type {
    ActiveFlag, AlertConfig,
    AlertConfigQueryParams, AsyncSendFlag, ChannelType, DefaultFlag, MessageContentFormat
} from '../types';

