/**
 * @deprecated 请改用 `RsUpload`（`@/ui`）。表单场景：RsDataForm `type: 'file'` + File[]。
 */
export { default as GFileUpload } from './GFileUpload.vue'
export type {
  FileInfo,
  FileUploadConfig,
  FileUploadMode,
  GFileUploadCallbacks,
  GFileUploadEmits,
  GFileUploadProps,
} from './types'
