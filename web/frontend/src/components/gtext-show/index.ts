/**
 * @deprecated GTextShow 已弃用，将逐步淘汰。新代码请直接使用 `RsCodeBlock`（@/ui）。
 */

export { default as GTextShow } from './GTextShow.vue'
/** @deprecated 随 GTextShow 淘汰；请改用 RsCodeBlock props（code / lang） */
export * from './types'

