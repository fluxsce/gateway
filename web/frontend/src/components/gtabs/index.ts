/**
 * GTabs 标签页导航（与 xirang-tabs 一致）
 * 仅导航栏，支持拖拽、关闭、右键菜单、溢出下拉，不依赖 Naive UI NTabs
 */

export { default as GTabs } from './GTabs.vue'
export type {
  GTabsProps,
  GTabsEmits,
  GTabsType,
  GTabsTabItem,
  GTabsInstance,
} from './types'
