/**
 * GContext 右键菜单组件
 */

export { default as GContext } from './GContext.vue'
export type {
  GContextMenuItem,
  GContextMenuItemType,
  GContextProps,
  GContextEmits,
  GContextInstance,
} from './types'
