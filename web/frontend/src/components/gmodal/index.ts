/**
 * @deprecated GModal 已弃用，将逐步淘汰。新代码请直接使用 `RsDialog`（@/ui）。
 * gexport-import 等业务弹窗已改为 RsDialog，勿再新增 GModal 依赖。
 */
export { default as GModal } from './GModal.vue'
/** @deprecated 随 GModal 淘汰；footer 请用 RsDialog #footer + RsButton */
export * from './types'
