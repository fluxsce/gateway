/**
 * 主题覆盖已迁移至 CSS token（styles/_theme-*.scss + styles/rs-brand.css）。
 * 保留空对象以兼容旧引用。
 */
export const lightThemeOverrides: Record<string, any> = {}
export const darkThemeOverrides: Record<string, any> = {}
